{
  "name": "parallax-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "PARALLAX — browser front end for the C++/WASM bitemporal engine",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b --pretty",
    "test": "vitest run",
    "test:watch": "vitest",
    "build:assets": "node scripts/build-nws-zones.mjs"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vite": "^6.0.7",
    "vitest": "^3.2.7"
  },
  "dependencies": {
    "maplibre-gl": "^6.1.0",
    "satellite.js": "^7.1.0"
  }
}
